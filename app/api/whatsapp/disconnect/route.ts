import { NextResponse } from 'next/server'; import { getUser } from '@/lib/auth'; import { db } from '@/lib/prisma';
export async function POST(req:Request){const user=await getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const {businessId}=await req.json().catch(()=>({}));if(!businessId)return NextResponse.json({error:'businessId is required'},{status:400});const membership=await db.membership.findUnique({where:{userId_businessId:{userId:user.id,businessId}}});if(!membership||!['OWNER','ADMIN'].includes(membership.role))return NextResponse.json({error:'Forbidden'},{status:403});await db.whatsAppAccount.deleteMany({where:{businessId}});await db.business.update({where:{id:businessId},data:{whatsappPhone:null}});return NextResponse.json({connected:false});}
