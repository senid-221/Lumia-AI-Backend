import { NextResponse } from 'next/server';
import { getUser } from '@/lib/auth'; import { db } from '@/lib/prisma'; import { indexKnowledgeItem } from '@/lib/embeddings'; import { z } from 'zod';
const schema=z.object({title:z.string().min(1).max(200),text:z.string().min(1).max(200000),source:z.string().max(500).optional()});
export async function POST(req:Request){const u=await getUser();if(!u)return NextResponse.json({error:'Unauthorized'},{status:401});const m=await db.membership.findFirst({where:{userId:u.id,role:{in:['OWNER','ADMIN']}}});if(!m)return NextResponse.json({error:'No business access'},{status:403});try{const b=schema.parse(await req.json());const item=await db.knowledgeItem.create({data:{businessId:m.businessId,title:b.title,content:b.text,source:b.source}});const index=await indexKnowledgeItem(item.id,m.businessId,item.title,item.content);return NextResponse.json({item,index},{status:201});}catch(e:any){return NextResponse.json({error:e?.issues?.[0]?.message||e?.message||'Invalid request'},{status:400});}}
